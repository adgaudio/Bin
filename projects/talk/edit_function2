

def comp():
    for x,y in zip(sorted(os.listdir('.')), sorted(os.listdir('../../transform/reports_sql'))):
        cmd=('diff %s ../../transform/reports_sql/%s' % (x,y))
        print cmd
        os.system(cmd)
        raw_input('continue?')



